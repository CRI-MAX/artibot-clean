# Web framework
flask>=2.0

# CORS support
flask-cors>=3.0

# OpenAI API client
openai>=1.0

# WSGI server per produzione (usato da Render)
gunicorn>=20.0

# Caricamento variabili da .env (opzionale, utile in locale)
python-dotenv>=1.0